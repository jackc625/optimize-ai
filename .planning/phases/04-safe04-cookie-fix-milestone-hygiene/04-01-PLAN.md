---
phase: 04-safe04-cookie-fix-milestone-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/page.tsx
  - .planning/phases/02-type-safety/02-02-SUMMARY.md
  - .planning/phases/03-test-infrastructure-quality/03-02-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SAFE-04

must_haves:
  truths:
    - "handleLogout in dashboard/page.tsx clears sb-authed cookie before router.push"
    - "All SUMMARY frontmatter uses consistent requirements-completed field name"
    - "REQUIREMENTS.md traceability table shows SAFE-04 as Complete"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Cookie-clearing handleLogout function"
      contains: "sb-authed=; path=/; expires=Thu, 01 Jan 1970 00:00:00 GMT"
    - path: ".planning/phases/02-type-safety/02-02-SUMMARY.md"
      provides: "Corrected requirements-completed frontmatter"
      contains: "requirements-completed"
    - path: ".planning/phases/03-test-infrastructure-quality/03-02-SUMMARY.md"
      provides: "Corrected requirements-completed frontmatter"
      contains: "requirements-completed"
  key_links:
    - from: "src/app/dashboard/page.tsx handleLogout"
      to: "src/middleware.ts sb-authed cookie check"
      via: "cookie cleared before router.push so middleware sees no cookie on next request"
      pattern: "sb-authed=;.*expires=Thu, 01 Jan 1970"
---

<objective>
Close the last audit gap (SAFE-04) by adding the missing sb-authed cookie clear to dashboard/page.tsx handleLogout, and normalize SUMMARY frontmatter field names to resolve documentation debt from the v1.0 milestone audit.

Purpose: Eliminate the brief window where middleware admits a signed-out user after logout from the dashboard page, and ensure all planning artifacts use consistent field naming.
Output: Patched dashboard/page.tsx, corrected SUMMARY frontmatter in 2 files, updated ROADMAP and REQUIREMENTS traceability.
</objective>

<execution_context>
@C:/Users/jackc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- The exact cookie-clearing pattern already used in layout.tsx line 64 -->
From src/app/dashboard/layout.tsx (handleLogout):
```typescript
const handleLogout = async () => {
  await supabase.auth.signOut();
  document.cookie = "sb-authed=; path=/; expires=Thu, 01 Jan 1970 00:00:00 GMT";
  router.push("/auth/login");
};
```

From src/middleware.ts (what reads the cookie):
```typescript
const isAuthed = request.cookies.get("sb-authed")?.value === "true";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sb-authed cookie clear to dashboard/page.tsx handleLogout</name>
  <files>src/app/dashboard/page.tsx</files>
  <read_first>
    - src/app/dashboard/page.tsx (current handleLogout at lines 63-66 — missing cookie clear)
    - src/app/dashboard/layout.tsx (reference handleLogout at lines 62-66 — has cookie clear)
    - src/middleware.ts (reads sb-authed cookie at line 5)
  </read_first>
  <action>
In `src/app/dashboard/page.tsx`, modify the `handleLogout` function (lines 63-66) to clear the `sb-authed` cookie before calling `router.push`. The current code is:

```typescript
const handleLogout = async () => {
  await supabase.auth.signOut();
  router.push("/auth/login");
};
```

Change it to:

```typescript
const handleLogout = async () => {
  await supabase.auth.signOut();
  document.cookie = "sb-authed=; path=/; expires=Thu, 01 Jan 1970 00:00:00 GMT";
  router.push("/auth/login");
};
```

This matches the exact pattern used in `dashboard/layout.tsx` line 64. The cookie must be cleared AFTER `signOut()` (so the sign-out completes) and BEFORE `router.push` (so middleware sees no cookie on the next request). No other changes to the file.
  </action>
  <verify>
    <automated>grep -n "sb-authed" src/app/dashboard/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/dashboard/page.tsx contains the string `sb-authed=; path=/; expires=Thu, 01 Jan 1970 00:00:00 GMT`
    - src/app/dashboard/page.tsx handleLogout has the cookie clear line between `supabase.auth.signOut()` and `router.push`
    - grep -c "sb-authed" src/app/dashboard/page.tsx returns 1
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>dashboard/page.tsx handleLogout clears sb-authed cookie before router.push, eliminating the window where middleware admits a signed-out user</done>
</task>

<task type="auto">
  <name>Task 2: Normalize SUMMARY frontmatter and update traceability docs</name>
  <files>
    .planning/phases/02-type-safety/02-02-SUMMARY.md
    .planning/phases/03-test-infrastructure-quality/03-02-SUMMARY.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/02-type-safety/02-02-SUMMARY.md (line 46: uses `requirements_satisfied` instead of `requirements-completed`)
    - .planning/phases/03-test-infrastructure-quality/03-02-SUMMARY.md (line 63: uses `requirements_satisfied` instead of `requirements-completed`)
    - .planning/phases/01-critical-safety/01-01-SUMMARY.md (line 37: canonical field name is `requirements-completed`)
    - .planning/ROADMAP.md (Phase 4 entry needs Plans count and plan list updated; Phase 4 Success Criteria item 2 needs update)
    - .planning/REQUIREMENTS.md (line 86: SAFE-04 traceability row shows Pending, needs update to Complete)
  </read_first>
  <action>
1. In `.planning/phases/02-type-safety/02-02-SUMMARY.md`, rename the frontmatter field on line 46 from `requirements_satisfied:` to `requirements-completed:`. The values (TYPE-02, TYPE-04, BUG-01) stay the same. This aligns with the field name used by all other SUMMARYs (01-01, 01-02, 01-03, 02-01, 02-03, 03-01).

2. In `.planning/phases/03-test-infrastructure-quality/03-02-SUMMARY.md`, rename the frontmatter field on line 63 from `requirements_satisfied:` to `requirements-completed:`. The values (QUAL-02, QUAL-03, QUAL-04) stay the same.

3. In `.planning/ROADMAP.md`:
   - Update Phase 4 Plans line from `**Plans**: TBD (via /gsd:plan-phase 4)` to `**Plans**: 1 plan`
   - Replace the line `Plans: TBD` (or add after Success Criteria) with:
     ```
     Plans:
     - [ ] 04-01-PLAN.md -- Add sb-authed cookie clear to handleLogout and normalize SUMMARY frontmatter
     ```
   - Update the Progress table row for Phase 4 from `0/?` to `0/1`

4. In `.planning/REQUIREMENTS.md`:
   - Line 86: Change `| SAFE-04 | Phase 4 | Pending |` to `| SAFE-04 | Phase 4 | Complete |`
   - Uncheck line 15: Change `- [x] **SAFE-04**:` ... wait, SAFE-04 is already checked. Verify and leave as-is if already checked.
  </action>
  <verify>
    <automated>grep -n "requirements-completed" .planning/phases/02-type-safety/02-02-SUMMARY.md .planning/phases/03-test-infrastructure-quality/03-02-SUMMARY.md && grep -c "requirements_satisfied" .planning/phases/02-type-safety/02-02-SUMMARY.md .planning/phases/03-test-infrastructure-quality/03-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/02-type-safety/02-02-SUMMARY.md contains `requirements-completed:` (not `requirements_satisfied:`)
    - .planning/phases/03-test-infrastructure-quality/03-02-SUMMARY.md contains `requirements-completed:` (not `requirements_satisfied:`)
    - grep -c "requirements_satisfied" across both files returns 0 for each
    - .planning/ROADMAP.md contains `04-01-PLAN.md`
    - .planning/ROADMAP.md Phase 4 Progress row shows `0/1`
    - .planning/REQUIREMENTS.md SAFE-04 traceability row shows `Complete` (not `Pending`)
  </acceptance_criteria>
  <done>All SUMMARY files use consistent `requirements-completed` field name; ROADMAP reflects 1 plan for Phase 4; REQUIREMENTS traceability updated</done>
</task>

</tasks>

<verification>
1. `grep -n "sb-authed" src/app/dashboard/page.tsx` -- must show the cookie clear line
2. `grep -rn "requirements_satisfied" .planning/phases/` -- must return 0 matches
3. `grep -n "requirements-completed" .planning/phases/02-type-safety/02-02-SUMMARY.md .planning/phases/03-test-infrastructure-quality/03-02-SUMMARY.md` -- must show field in both files
4. `npx tsc --noEmit` -- exits 0 (no type errors introduced)
5. `npx vitest run` -- all 31 tests pass (no regressions)
</verification>

<success_criteria>
- handleLogout in dashboard/page.tsx clears sb-authed cookie before router.push (matching layout.tsx pattern exactly)
- Zero instances of `requirements_satisfied` remain in any SUMMARY file
- REQUIREMENTS.md shows SAFE-04 as Complete in traceability table
- All existing tests pass, no type errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-safe04-cookie-fix-milestone-hygiene/04-01-SUMMARY.md`
</output>
